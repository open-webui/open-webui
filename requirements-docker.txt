# Docker-specific requirements for mAI deployment

# Include all backend requirements
-r backend/requirements.txt

# Additional production dependencies
gunicorn==23.0.0
supervisor==4.2.5

# Health check utilities
healthcheck>=1.3.3

# Monitoring and metrics
prometheus-client>=0.20.0

# Production optimizations
orjson>=3.10.0  # Faster JSON parsing
uvloop>=0.19.0  # Faster event loop for asyncio

# Security
python-multipart>=0.0.20
secure>=0.3.0

# Database connection pooling
asyncpg>=0.29.0  # For PostgreSQL if migrating from SQLite
databases>=0.8.0

# Rate limiting
slowapi>=0.1.9

# CORS and security headers
secure>=0.3.0

# Production logging
python-json-logger>=2.0.7