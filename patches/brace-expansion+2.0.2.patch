diff --git a/node_modules/brace-expansion/index.js b/node_modules/brace-expansion/index.js
index 2f3a5cc..4c9e5d9 100644
--- a/node_modules/brace-expansion/index.js
+++ b/node_modules/brace-expansion/index.js
@@
-// original vulnerable regex
-var re = /^(.*)\.\.(.*)$/;
-var m  = str.match(re);
+// PATCH – mitigate ReDoS (GHSA-5447-rq35-17mx)
+var re = /^([^\n]*)\.\.([^\n]*)$/;
+var m  = re.exec(str);
 
   if (!m) return [str];
 
-  var a = +m[1];
-  var b = +m[2];
+  var a = +m[1];
+  var b = +m[2];
+  // guard huge ranges
+  if (Math.abs(b - a) > 1000) return [str];
 
-  var result = [];
-  for (var i = a; i <= b; i++) {
+  var result = [];
+  for (var i = a; i <= b; i++) {
     result.push(String(i));
   }
   return result;
 }
