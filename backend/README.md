# Backend for Open WebUI

To run the development version, run in a uv environment:

```sh
brew install uv
uv venv
uv pip install -r requirements.txt
uv run dev.sh

# to enter the virtual environment
source ./venv/bin/activate
# to exit
deactivate
```
