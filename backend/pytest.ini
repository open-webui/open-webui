[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Same basenames across suites (e.g. test_router.py) need importlib mode.
addopts = --import-mode=importlib
