services:
#  ollama:
#    volumes:
#      - ollama:/root/.ollama
#    container_name: ollama
#    pull_policy: always
#    tty: true
#    restart: unless-stopped
#    image: ollama/ollama:${OLLAMA_DOCKER_TAG-latest}

  open-webui:
#    build:
#      context: .
#      dockerfile: Dockerfile
    image: openwebui/open-webui:0.9.1  # ghcr.io/open-webui/open-webui:${WEBUI_DOCKER_TAG-main}
    container_name: open-webui

#    depends_on:
#      - ollama
    network_mode: bridge
    ports:
#      - ${OPEN_WEBUI_PORT-3000}:8080
      - 9180:8080
    volumes:
      - ./data:/app/backend/data
      - ./build:/app/build
    environment:
      - 'OLLAMA_BASE_URL=http://ollama:11434'
      - 'WEBUI_SECRET_KEY='
#    extra_hosts:
#      - host.docker.internal:host-gateway
    restart: unless-stopped

#volumes:
#  ollama: {}
#  open-webui: {}

