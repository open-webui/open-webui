services:
  lanyuechat:
    build:
      context: .
      dockerfile: Dockerfile
    image: harbor.houmo.ai/application_software/lanyuechat:v1.1.0
    container_name: lanyuechat
    volumes:
      - ./data/backend:/app/backend/data
    # ports:
    #   - 8901:8901
    env_file:
      - .env
    # extra_hosts:
    #   - host.docker.internal:host-gateway
    network_mode: host
    restart: unless-stopped
