# Unit test
```
Generate unit test cases for the following function using [Jest/Pytest/JUnit]:
[Function code or specification visible in Cursor AI]

Requirements:
- Cover positive scenarios (valid inputs, expected behavior)
- Cover negative scenarios (invalid inputs, error handling)
- Include edge cases (boundary values, empty sets, maximum values)
- For each test case, specify:
  * Test ID and description
  * Test inputs
  * Expected outputs
  * Preconditions (if any)"
```